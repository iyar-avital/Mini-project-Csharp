<UserControl x:Class="PLWPF.userControlush"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:local="clr-namespace:PLWPF"
             mc:Ignorable="d" Width="200" Height="120">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Label Content="Enter your password" Margin="3,0,26,0" VerticalAlignment="Top" Height="35" FontSize="16" FontWeight="Bold"/>
        <PasswordBox ToolTip="Your password is the last 4 digits of your ID" HorizontalAlignment="Left" Name="passwordMask"  Height="27" Margin="11,36,0,0" MaxLength="4" PasswordChar="*" VerticalAlignment="Top" Width="147"/>
        <Button Content="Log-in" Background="White" FontWeight="Bold" Foreground="DeepPink" BorderBrush="White" HorizontalAlignment="Left" Margin="33,68,0,0" VerticalAlignment="Top" Width="100" Height="29" RenderTransformOrigin="-0.414,-1.2" Click="Button_Click"/>
    </Grid>
</UserControl>
